What replaces the confirmation once the stored action ran.

```jsx
<ReceiptCard title="Refund issued" amountCents={4500} tone="out" description={<>to Maya Chen · <span className="ldg-mono">re_3Qk2…</span></>} status="succeeded" />
```