Every dollar amount in the product; cents in, formatted mono out.

```jsx
<Figure cents={241200} size="xl" tone="in" splitCents />   // hero
<Figure cents={4500} size="lg" tone="out" sign="−" />        // refund
```
Green = money in, coral = money out, ink = neutral.