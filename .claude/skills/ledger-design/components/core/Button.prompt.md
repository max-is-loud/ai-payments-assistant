Pill-shaped button; primary is solid ink, secondary is a hairline outline, danger is a small coral outline used only for Retry.

```jsx
<Button>Approve refund</Button> <Button variant="secondary">Cancel</Button> <Button variant="danger">Retry</Button>
<Button block size="lg">Approve and notify</Button>
```